# homebrew-tap

```bash
# quit ArmorText
osascript -e 'quit app "ArmorText"'
# remove ArmorText
rm -rf /Applications/ArmorText.app
# install ArmorText from our tap via brew
brew install armortext/tap/armortext-desktop
```
